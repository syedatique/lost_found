<% if can? :create, Found %>
  <%= link_to new_found_path, class: 'btn btn-success' do %>
    <span class="glyphicon glyphicon-plus" aria-hidden="true"> Post New AD </span>
  <% end %>
<% end %>

<%= render @founds %>

<div class="thumbnail" style="border:none;">
  <%= will_paginate @founds, class: 'btn btn-info pull-right' %>
  <%= link_to 'Back', founds_path, class: 'btn btn-info pull-left' %>
</div>
